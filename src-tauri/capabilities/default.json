{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the SpecHub desktop window, including event listening for live document updates.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
